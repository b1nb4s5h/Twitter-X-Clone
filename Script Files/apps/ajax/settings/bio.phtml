<?php
require_once("../../../core/vipub_web_core.phtml");

if (!isset($_SESSION["user_id"])) {
  vipub_redirect("/welcome/login");
  exit;
}
$user_id = mysqli_real_escape_string($conn, htmlspecialchars($_SESSION["user_id"]));
$user_bio = mysqli_real_escape_string($conn, strip_tags($_POST['user_bio']));
$sql = "UPDATE vipub_users SET user_bio='$user_bio' WHERE id=$user_id";
if (mysqli_query($conn, $sql)) {
    echo $user_bio;
} else {
    echo "Hata: " . mysqli_error($conn);
}

mysqli_close($conn);