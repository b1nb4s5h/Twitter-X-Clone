<?php
require_once("../../../core/vipub_web_core.phtml");

if (!isset($_SESSION["user_id"])) {
  vipub_redirect("/welcome/login");
  exit;
}

// Kaydedilmiş tema ayarını yükle
$user_id = $_SESSION["user_id"];
$sql = "SELECT theme_color FROM vipub_users WHERE id = ?";
$stmt = mysqli_prepare($conn, $sql);
mysqli_stmt_bind_param($stmt, "i", $user_id);
mysqli_stmt_execute($stmt);
$result = mysqli_stmt_get_result($stmt);

if (mysqli_num_rows($result) > 0) {
    $row = mysqli_fetch_assoc($result);
    $savedTheme = $row["theme_color"];
} else {
    $savedTheme = null;
}

mysqli_close($conn);

// Kaydedilmiş tema ayarını döndür
echo $savedTheme;