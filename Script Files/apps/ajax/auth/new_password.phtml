<?php
require_once("../../../core/xclone_web_core.phtml");
if ($_SERVER['REQUEST_METHOD'] == 'POST') {
    $password = sha1($_POST["password"]);
    $query = "UPDATE xclone_users SET password='$password', reset_token=NULL WHERE id = '".$_GET["user"]."'";
    if ($conn->query($query) === TRUE) {
      echo "".xclone_translate("settings_page_save_value_desc_16")."";
    } else {
      echo "Error: " . $conn->error;
    }
  
    $conn->close();
} else {
    echo "Not db.";
}

